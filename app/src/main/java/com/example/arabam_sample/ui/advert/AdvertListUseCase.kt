package com.example.arabam_sample.ui.advert

class AdvertListUseCase {
    /* fun fetchAdvertList(skip: Int, take: Int, sortModel: SortModel) =
         advertRepository.fetchAdvert(skip, take, sortModel) */
}